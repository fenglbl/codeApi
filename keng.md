# keng.md

## 已记录坑位

### 1. 聊天页流式输出看起来像“整段一起出来”
- 状态：暂缓处理
- 现象：CodeAip 管理端聊天页里，助手回复虽然启用了流式模式，但用户侧观感仍然像最后整段一次性渲染出来，而不是真正逐步输出。
- 当前判断：后端链路大概率不是主因，因为同一后端接入 OpenClaw 时可以正常流式输出。
- 更可能的问题位置：CodeAip 管理端聊天页前端的 SSE 读取、拆包、队列消费或渲染节奏。
- 备注：后续继续排查时，优先从前端 `ChatView.vue` 的流式读取与渲染逻辑入手，不要先动后端。
